package home.datastructures;

public class MyHashMap {
	
	
	//intial capacity
	// Array of Entry
	
	//construction
		
	
	//put
		//create the hash for the key
		// convert that to array index
		// check if entry already exists at that index
		// loop through the linked list to see if key already exists (with equals() method)
		// if yes --> overwrite the value
		// if no --> add an Entry to the linkedList
	
	
	//get
		// create the hash for the key
		// convert that to the array index
		// check if entry available at that index
		// loop through the linked list to see if the key already exists (with equals() method)
		// if found --> return the value
		// else --> null
	
	
	
	// Entry class
		// key
		// value

}
